import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export const TeamAttendance: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Team Attendance</Title>
      <p>Team Attendance component - Coming soon!</p>
    </Card>
  );
};
