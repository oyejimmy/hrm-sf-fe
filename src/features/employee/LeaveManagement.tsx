import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export const LeaveManagement: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Leave Management</Title>
      <p>Leave Management component - Coming soon!</p>
    </Card>
  );
};
