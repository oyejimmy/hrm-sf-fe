import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export const Documents: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Documents</Title>
      <p>Documents component - Coming soon!</p>
    </Card>
  );
};
