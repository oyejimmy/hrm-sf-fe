import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export const Attendance: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Attendance</Title>
      <p>Attendance component - Coming soon!</p>
    </Card>
  );
};
