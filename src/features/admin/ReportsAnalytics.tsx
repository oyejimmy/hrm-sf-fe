import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export const ReportsAnalytics: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Reports & Analytics</Title>
      <p>Reports & Analytics component - Coming soon!</p>
    </Card>
  );
};
