import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export const CommunicationAndNotification: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Communication & Notifications</Title>
      <p>Communication & Notifications component - Coming soon!</p>
    </Card>
  );
};
