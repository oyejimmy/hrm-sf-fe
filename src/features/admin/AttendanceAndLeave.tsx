import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

export const AttendanceAndLeave: React.FC = () => {
  return (
    <Card>
      <Title level={2}>Attendance & Leave</Title>
      <p>Attendance & Leave component - Coming soon!</p>
    </Card>
  );
};
